# Chapter 28 - Admin Only Endpoints

ตัวอย่างนี้ใช้ประกอบบทที่ 28 สำหรับสร้าง endpoint ที่เข้าได้เฉพาะ admin

ไฟล์ที่ใช้:

- `SecurityConfig.java`
- `AdminController.java`
- `requests.http`

ต้องใช้ร่วมกับ JWT config จากบทที่ 24 และ role claim จากบทที่ 27

