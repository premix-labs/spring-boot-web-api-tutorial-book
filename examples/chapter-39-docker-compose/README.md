# Chapter 39 - Docker และ Docker Compose

ตัวอย่างนี้ใช้ประกอบบทที่ 39 สำหรับรัน Spring Boot API และ PostgreSQL ด้วย Docker Compose

ไฟล์ที่ใช้:

- `Dockerfile`
- `docker-compose.yml`
- `.env.example`

ก่อนรัน compose ให้ build jar:

```powershell
.\gradlew.bat clean build
```
