# Chapter 25 - Current User / Me

ตัวอย่างนี้ใช้ประกอบบทที่ 25 สำหรับเพิ่ม endpoint `/api/v1/auth/me`

ไฟล์ที่ใช้:

- `AuthController.java`
- `AuthService.java`
- `requests.http`

ต้องใช้ร่วมกับ JWT config จากบทที่ 24
