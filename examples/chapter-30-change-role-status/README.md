# Chapter 30 - เปลี่ยน Role และ Status

ตัวอย่างนี้ใช้ประกอบบทที่ 30 สำหรับให้ admin เปลี่ยน role และ status ของผู้ใช้

ไฟล์ที่ใช้:

- `ChangeRoleRequest.java`
- `ChangeStatusRequest.java`
- `AdminUserService.java`
- `AdminUserController.java`
- `GlobalExceptionHandler.java`
- `requests.http`

บทนี้ยังไม่บันทึก audit log ตัวอย่าง audit log จะอยู่ในบทที่ 31
