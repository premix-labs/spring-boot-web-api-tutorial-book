# Chapter 26 - Protect Endpoints

ตัวอย่างนี้ใช้ประกอบบทที่ 26 สำหรับกำหนด public/protected/admin endpoint

ไฟล์ที่ใช้:

- `SecurityConfig.java`
- `AdminController.java`
- `requests.http`

ต้องใช้ร่วมกับ JWT config, AuthController และ AuthService จากบทก่อนหน้า

