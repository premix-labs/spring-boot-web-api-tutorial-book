# Chapter 22 - Hash Password

ตัวอย่างนี้ใช้ประกอบบทที่ 22 สำหรับ hash password ด้วย `PasswordEncoder`

ไฟล์ที่ใช้:

- `build.gradle-snippet.gradle`
- `SecurityBeansConfig.java`
- `AuthService.java`
- `requests.http`

หลังทำบทนี้ password ใน database ต้องไม่ใช่ plain text
